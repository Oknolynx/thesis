\chapter{Conclusion}
\label{chap:conclusion}

\todo{from \autoref{chap:ourapproach.rejected.wdf}}: ``Now that our WDM driver is finished, it would be interesting to compare it to an equivalent that was written using KMDF, both its architecture and performance. This is left as an exercise to the curious reader; \autoref{chap:ourapproach.final} should be a good starting point.''

\todo{store config in registry, don't attach to non-LUKS2 volumes through that?} also mentioned in \autoref{chap:ourapproach.final.init}

\todo{from \autoref{chap:ourapproach.security}: luks2filterstart.exe does not clear its memory}

\todo{make some of the bibliography entries shorter/nicer}